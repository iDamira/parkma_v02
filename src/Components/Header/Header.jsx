import React from 'react';
import Navbar from '../Navbar/Navbar';
import './Header.css';

const Header = props => (
  <div id="header-container">
    <Navbar />
    Hello, I am Header!
  </div>
);

export default Header;
